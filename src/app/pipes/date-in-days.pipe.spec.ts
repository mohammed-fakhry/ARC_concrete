import { DateInDaysPipe } from './date-in-days.pipe';

describe('DateInDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new DateInDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
